# stark-hamlet-59828
setup:
  addons:
    - plan: heroku-postgresql
    - plan: heroku-redis
build:
  docker:
    web: exchange/Dockerfile
    celery: exchange/Dockerfile
run:
  web: python manage.py runserver 0.0.0.0:8000
  worker: celery -A exchange worker -l INFO
